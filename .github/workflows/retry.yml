name: Oracle A1 Batam Retry

on:
  schedule:
    - cron: '*/15 * * * *'  # tiap 15 menit
  workflow_dispatch:

concurrency:
  group: oracle-retry
  cancel-in-progress: true

jobs:
  try-create-instance:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - name: Cache OCI CLI
        id: cache-oci
        uses: actions/cache@v4
        with:
          path: ~/.local
          key: ${{ runner.os }}-oci-cli-v2

      - name: Setup OCI CLI
        run: |
          if [ "${{ steps.cache-oci.outputs.cache-hit }}" != "true" ]; then
            pip install oci-cli --quiet
          fi
          echo "$HOME/.local/bin" >> $GITHUB_PATH

          mkdir -p ~/.oci ~/.ssh
          echo "${{ secrets.OCI_CLI_KEY_CONTENT }}" > ~/.oci/oci_api_key.pem
          chmod 600 ~/.oci/oci_api_key.pem

          cat > ~/.oci/config <<EOF
          [DEFAULT]
          user=${{ secrets.OCI_CLI_USER }}
          tenancy=${{ secrets.OCI_CLI_TENANCY }}
          fingerprint=${{ secrets.OCI_CLI_FINGERPRINT }}
          key_file=~/.oci/oci_api_key.pem
          region=ap-batam-1
          EOF

          echo "${{ secrets.SSH_PUBLIC_KEY }}" > ~/.ssh/id_rsa.pub

      - name: Cek instance A1 sudah ada
        id: check
        run: |
          EXISTING=$(oci compute instance list \
            --compartment-id "${{ secrets.OCI_TENANCY_OCID }}" \
            --lifecycle-state RUNNING \
            --auth api_key \
            --no-retry 2>&1 || true)
          if echo "$EXISTING" | grep -q "A1.Flex"; then
            echo "exists=true" >> $GITHUB_OUTPUT
          else
            echo "exists=false" >> $GITHUB_OUTPUT
          fi

      - name: Instance sudah ada — nonaktifkan workflow
        if: steps.check.outputs.exists == 'true'
        run: |
          curl -s "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
            -d "chat_id=${{ secrets.TELEGRAM_CHAT_ID }}" \
            --data-urlencode "text=✅ Oracle A1 Batam sudah RUNNING! Workflow dihentikan otomatis. Waktu: $(date '+%Y-%m-%d %H:%M UTC')"
          curl -s -X PUT \
            -H "Authorization: Bearer ${{ secrets.GH_PAT }}" \
            -H "Accept: application/vnd.github+json" \
            "https://api.github.com/repos/${{ github.repository }}/actions/workflows/retry.yml/disable"

      - name: Coba buat instance A1
        id: launch
        if: steps.check.outputs.exists == 'false'
        run: |
          # 5 strategi optimal: 4cpu semua FD (prioritas) + 2cpu-auto (fallback)
          # Urutan: auto dulu (biar Oracle pilih FD terbaik), lalu eksplisit per FD
          # 1cpu tidak dimasukkan — tidak bisa resize ke 4cpu tanpa delete ulang
          LABELS=("4cpu-auto" "4cpu-fd1" "4cpu-fd2" "4cpu-fd3" "2cpu-auto")
          OCPUS=(4            4          4          4          2)
          MEMS=(24            24         24         24         12)
          FDS=(""             "FAULT-DOMAIN-1" "FAULT-DOMAIN-2" "FAULT-DOMAIN-3" "")

          SUCCESS=false
          for i in "${!LABELS[@]}"; do
            LABEL="${LABELS[$i]}"
            OCPU="${OCPUS[$i]}"
            MEM="${MEMS[$i]}"
            FD="${FDS[$i]}"
            NAME="batam-gh-$(date +%m%d%H%M)-$$"

            echo "[$((i+1))/5] Mencoba $LABEL (${OCPU} OCPU, ${MEM}GB) → $NAME"

            OCI_ARGS=(
              compute instance launch
              --availability-domain "GsbX:AP-BATAM-1-AD-1"
              --compartment-id "${{ secrets.OCI_TENANCY_OCID }}"
              --shape "VM.Standard.A1.Flex"
              --shape-config "{\"ocpus\":${OCPU},\"memoryInGBs\":${MEM}}"
              --image-id "ocid1.image.oc1.ap-batam-1.aaaaaaaansnq2iiriekoejkn2ktdnl5s553mj3yrqgnib3z77suae3v6ypfq"
              --subnet-id "ocid1.subnet.oc1.ap-batam-1.aaaaaaaajpdq76idwvc77ciys7go33rpm2ulqdmwsk2v5c66oc2kw45pllva"
              --assign-public-ip true
              --ssh-authorized-keys-file ~/.ssh/id_rsa.pub
              --display-name "$NAME"
              --auth api_key
              --no-retry
            )

            if [ -n "$FD" ]; then
              OCI_ARGS+=(--fault-domain "$FD")
            fi

            RESULT=$(oci "${OCI_ARGS[@]}" 2>&1 || true)

            if echo "$RESULT" | grep -q '"lifecycle-state": "PROVISIONING"'; then
              echo "✅ BERHASIL dengan $LABEL!"
              echo "success=true" >> $GITHUB_OUTPUT
              echo "label=$LABEL" >> $GITHUB_OUTPUT
              echo "name=$NAME" >> $GITHUB_OUTPUT
              SUCCESS=true
              break
            elif echo "$RESULT" | grep -qi "LimitExceeded\|limit_exceeded"; then
              echo "LimitExceeded — instance mungkin sudah ada (stopped/starting)."
              echo "success=limit" >> $GITHUB_OUTPUT
              SUCCESS=true
              break
            elif echo "$RESULT" | grep -qi "TooManyRequests\|Too many requests"; then
              echo "⚠️ Rate-limited Oracle — berhenti, tunggu run berikutnya."
              break
            else
              ERR=$(echo "$RESULT" | grep -oE '"message": "[^"]*"' | head -1 || echo "(no message)")
              echo "Gagal ($LABEL): $ERR"
              # Jeda 30s agar lolos rate-limit Oracle sebelum strategi berikutnya
              if [ $i -lt 4 ]; then sleep 30; fi
            fi
          done

          if [ "$SUCCESS" = false ]; then
            echo "success=false" >> $GITHUB_OUTPUT
          fi

      - name: Kirim notifikasi Telegram (berhasil)
        if: steps.launch.outputs.success == 'true'
        run: |
          curl -s "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
            -d "chat_id=${{ secrets.TELEGRAM_CHAT_ID }}" \
            --data-urlencode "text=🎉 Oracle A1 Batam BERHASIL! Instance: ${{ steps.launch.outputs.name }} | Shape: ${{ steps.launch.outputs.label }} | Waktu: $(date '+%Y-%m-%d %H:%M UTC')"

      - name: Nonaktifkan workflow setelah berhasil
        if: steps.launch.outputs.success == 'true' || steps.launch.outputs.success == 'limit'
        run: |
          curl -s -X PUT \
            -H "Authorization: Bearer ${{ secrets.GH_PAT }}" \
            -H "Accept: application/vnd.github+json" \
            "https://api.github.com/repos/${{ github.repository }}/actions/workflows/retry.yml/disable"

      - name: Kirim notifikasi masih penuh (1x per jam)
        if: steps.launch.outputs.success == 'false'
        run: |
          # Kirim hanya tiap 4 run (~1 jam) agar tidak spam Telegram
          if (( ${{ github.run_number }} % 4 == 0 )); then
            curl -s "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
              -d "chat_id=${{ secrets.TELEGRAM_CHAT_ID }}" \
              --data-urlencode "text=⏳ A1 Batam masih penuh (run #${{ github.run_number }}). Waktu: $(date '+%Y-%m-%d %H:%M UTC'). Bot aktif retry tiap 15 menit."
          else
            echo "Run #${{ github.run_number }} — skip notif (kirim tiap 4 run / ~1 jam)"
          fi
